name: Publication update notification

on:
  push:
    branches: [master]
    paths:
      - "index.html"
      - "publications/**"
      - "zh/publications/**"
      - "research-notes/**"
      - "research/**"
      - "zh/research/**"
      - "sitemap.xml"

permissions:
  contents: read

jobs:
  submit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Notify discovery service
        run: python _notify_updates.py
